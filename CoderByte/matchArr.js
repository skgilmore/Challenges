function FindIntersection(strArr) {

    const arrOne = strArr[0].split(',');
    const arrTwo = strArr[1].split(',');
    const matchArr = [];

    const itemArr1 = (arrOne) => {
        item = parseInt(arrOne)
    }
    const itemArr2 = () => {
        item = parseInt(arrTwo)
    }

    const matchedItem = () => {
        if (itemArr1 === itemArr2) {
            matchArr.push(matchedItem)
            {

                return matchArr;
            }

            // code goes here  
        }
        // console.log(itemArr1, "readitem")
    }
}

// keep this function call here 
console.log(FindIntersection(readline()));